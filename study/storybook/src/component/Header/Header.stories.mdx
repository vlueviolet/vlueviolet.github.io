import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Header from './Header';

<Meta title="Example/Header" component={Header} />

export const Template = (args) => <Header {...args} />;

<Canvas>
  <Story name="Basic" args={{gnbList: [{label: 'test1'}, {label: 'test2'}]}}>
      {Template.bind({})}
  </Story>
</Canvas>

# 안녕하세요

<ArgsTable of={Header} />
