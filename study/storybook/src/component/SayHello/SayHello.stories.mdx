import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Title,
  Subtitle,
  Description,
  Primary,
  Stories,
  IconGallery
} from '@storybook/addon-docs/blocks';
import { TabsState, Button, Badge, StorybookIcon } from '@storybook/components';
import SayHello from './SayHello';

<Meta title="Component/SayHello" component={SayHello} />

export const getCaptionForLocale = (locale) => {
  switch (locale) {
    case 'en':
      return 'Hello';
    case 'fr':
      return 'Bonjour';
    case 'zh':
      return '你好';
    case 'kr':
      return '안녕하세요';
  }
};

export const Template = (args, { globals: { locale } }) => (
  <SayHello {...{ ...args, label: getCaptionForLocale(locale) }} />
);

<Title />

<Subtitle />

<Description />

<Primary />

<Stories />

<StorybookIcon width="100px" />

<TooltipNote note="TooltipNote" />

<Button primary disabled>
  button primary
</Button>

<Badge status="neutral">bedge neutral</Badge>

<TabsState initial="yarn">
  <div id="yarn" title="Yarn">
    <div>```sh yarn add -D storybook-addon-designs ```</div>
  </div>
  <div id="npm" title="NPM">
    <div>```sh npm i -D storybook-addon-designs ```</div>
  </div>
</TabsState>

# 안녕하세요

<ArgsTable of={SayHello} />
