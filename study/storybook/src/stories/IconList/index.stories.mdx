import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  ColorPalette,
  ColorItem
} from '@storybook/addon-docs/blocks';

import IconList from './';

<Meta title='Icons/svg/all' component={IconList} />

export const Template = (args) => <IconList {...args} />;

## Icon List

> 프로젝트에서 사용되는 `svg` 정보를 볼 수 있습니다.

<Canvas>
  <Story name='list' parameters={{ options: { showPanel: false } }}>
    {Template.bind({})}
  </Story>
</Canvas>
