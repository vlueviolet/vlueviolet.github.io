import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  ColorPalette,
  ColorItem
} from '@storybook/addon-docs/blocks';
import { IconGallery } from 'storybook-icon-gallery';
import IconSvgCloud from 'asset/images/svg/icon-cloud.svg';
import IconSvgForm from 'asset/images/svg/icon-form.svg';
import IconSvgHome from 'asset/images/svg/icon-home.svg';
import IconSvgMail from 'asset/images/svg/icon-mail.svg';
import IconSvgMenu from 'asset/images/svg/icon-menu.svg';
import IconSvgMessage from 'asset/images/svg/icon-message.svg';
import IconSvgOffice from 'asset/images/svg/icon-office.svg';
import IconPngLogo from 'asset/images/png/logo.png';

import Icon from './index';

export const IconArgsType = {
  IconSvgElement: {
    table: { disable: true }
  },
  color: { control: { type: 'color' } }
};

export function renderItem(
  name,
  Icon1,
  Size1 = 25,
  Color1 = '#000',
  Icon2,
  Size2 = 25,
  Color2 = '#000'
) {
  return (
    <IconGallery.Variants name={name}>
      <IconGallery.Variant size={Size1}>
        {Icon1 && <Icon1 color={Color1} />}
      </IconGallery.Variant>
      <IconGallery.Variant size={Size2}>
        {Icon2 && <Icon2 color={Color2} />}
      </IconGallery.Variant>
    </IconGallery.Variants>
  );
}

<Meta title='Icons/svg/separate' component={Icon} argTypes={IconArgsType} />

# SVG Viewer

> svg 파일을 웹에서 볼 수 있고, `width`, `height`, `color`를 테스트 할 수 있습니다.

<br />
<br />

export const Template = (args) => <Icon {...args} />;

## Icon 컴포넌트를 이용한 SVG Viewer

<Canvas>
  <Story
    name='icon-cloud'
    args={{ IconSvgElement: IconSvgHome }}
    parameters={{ a11y: { disable: true }, actions: { disable: true } }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='icon-form'
    args={{ IconSvgElement: IconSvgForm }}
    parameters={{ a11y: { disable: true }, actions: { disable: true } }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='icon-home'
    args={{ IconSvgElement: IconSvgHome }}
    parameters={{ a11y: { disable: true }, actions: { disable: true } }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='icon-mail'
    args={{ IconSvgElement: IconSvgMail }}
    parameters={{ a11y: { disable: true }, actions: { disable: true } }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='icon-menu'
    args={{ IconSvgElement: IconSvgMenu }}
    parameters={{ a11y: { disable: true }, actions: { disable: true } }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='icon-message'
    args={{ IconSvgElement: IconSvgMessage }}
    parameters={{ a11y: { disable: true }, actions: { disable: true } }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='icon-office'
    args={{ IconSvgElement: IconSvgOffice }}
    parameters={{ a11y: { disable: true }, actions: { disable: true } }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props Table

<ArgsTable of={Icon} />
